mixin text-field({type = "text", jsClass = "test", placeholder = "Email"} = {})
  -
    let className
    if(jsClass === "test")
      className = 'text-field'
    else
      className = `text-field js-${jsClass}-text-field`
  input(class = className, type = type, placeholder = placeholder)