mixin subscription-field({placeholder = "Email"} = {})
  .subscribe--container
    input(type="Email", class = "text-field", placeholder = placeholder)
    button(type="submit", class = "subscribe--button js-subscribe--button") arrow_forward