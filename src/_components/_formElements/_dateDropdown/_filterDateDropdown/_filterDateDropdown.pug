mixin filterDateDropdown(id, label)
        label(for = id)
            h3(class = "label") #{label}
        input(type="text", id = id, class = "textField dateDropdown", placeholder = "Выберите даты пребывания")