mixin rateButton(id)
    -
        var rateId = ['', '', '', '', '']
        for (var i = 1; i <= 5; i++)
            rateId[i-1] = "rateButton__" + i + "__" + id
    div(style="display: flex;")
        each val in rateId
            button(type = "button", class = "buttons__rate buttons__rate_normal", id = val)

